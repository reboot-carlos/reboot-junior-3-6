# Copie ce fichier en ".env" (sans ".example") et remplis tes vraies valeurs.
# Le fichier ".env" ne doit JAMAIS être envoyé sur GitHub (il est ignoré,
# voir le .gitignore à la racine).

# Clé API Claude (https://console.anthropic.com/account/keys)
ANTHROPIC_API_KEY=sk-ant-...
